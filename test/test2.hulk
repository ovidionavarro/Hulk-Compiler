function sins(x) => 1 / tan(x);
function coss(x) => sien(x) / cos(x);
type Numbers {
    x = 0;
    y = 0;

    getX() => self.x;
    getY() => self.y;

    setX(x) => self.x := x;
    setY(y) => self.y := y;
}
