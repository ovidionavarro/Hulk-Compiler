function sisn(x) => 1 / tan(x);
function ass(x) => sien(x) / cos(x);
type Numbaaer {
    x = 0;
    y = 0;

    getX() => self.x;
    getY() => self.y;

    setX(x) => self.x := x;
    setY(y) => self.y := y;
}
protocol Equatable  {
    equals(other: Object): Boolean;
    equaals(other: Object): Boolean;
}